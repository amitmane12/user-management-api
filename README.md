# Enterprise standard user management api

Phase 1
User Registration
Login
Logout
Refresh Token
Get Profile
Update Profile
Change Password

steps followd:
1. install express
    `npm install express`

    install typescript as dev dep
    `npm install --save-dev typescript @types/express @types/node`

2. configure typescript
    `npx tsc --init`

    `npm i -D tsx`